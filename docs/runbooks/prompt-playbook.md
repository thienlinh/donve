# Prompt playbook — build nền tảng từ đầu đến cuối

> Đi kèm `ai-guide.md` (cách Claude tự detect agent/skill) và `implementation-plan.md` (phase, effort, DoD). File này là phần **còn thiếu**: danh sách prompt cụ thể, đúng thứ tự, để chạy từng phase mà không phải tự nhớ nêu context gì mỗi lần.

## Trả lời trước: Claude Code có "folder prompts" không?

Không có cơ chế nào tự nạp một `prompts/` tuỳ ý — chỉ có 2 folder Claude Code thực sự đọc:

- **`.claude/agents/*.md`** — định nghĩa agent (đã có, không phải chỗ để chứa prompt xây tính năng).
- **`.claude/commands/*.md`** — mỗi file = 1 **slash command** tái sử dụng (gõ `/ten-file`), có tham số. Đúng chỗ cho prompt bạn sẽ **gõ lại nhiều lần** (vd "thêm product type mới", "thêm skill nền tảng mới").

Danh sách prompt build từ đầu-đến-cuối trong file này **không** phải loại đó — mỗi prompt chỉ chạy **một lần**, theo thứ tự phase, không cần tham số hoá. Biến nó thành slash command là over-engineering (bọc thêm cơ chế cho việc chỉ chạy 1 lần). Vì vậy: giữ nguyên dạng **doc thường trong `docs/`** — đúng convention đang có (`ops/implementation-plan.md` đã là "kế hoạch", file này là "kế hoạch ở mức prompt"), đặt cạnh `ai-guide.md` vì cùng chủ đề "prompt Claude Code trong repo này".

Nếu về sau có việc lặp lại thật (vd cứ mỗi campaign mới lại phải gõ lại một đoạn prompt dài), lúc đó mới tách riêng thành `.claude/commands/`.

## Cách dùng file này

1. Chạy đúng thứ tự phase 0 → 7 (không nhảy cóc — mỗi phase là nền cho phase sau, đúng tinh thần "không MVP cắt gọt" của implementation-plan.md).
2. Copy nguyên văn prompt, dán vào Claude Code. Prompt đã cố ý nêu rõ **quy mô** và **agent kỳ vọng** theo mẹo ở `ai-guide.md` — không cần gõ tên agent thủ công, nhưng có thể ép trực tiếp nếu muốn.
3. Sau mỗi prompt: chạy `bun run lint && bun run typecheck && bun run build` (đã là rule bắt buộc ở `.claude/rules/tech-stack.md`), rồi đối chiếu **DoD** của phase trong implementation-plan.md trước khi sang prompt kế.
4. Mỗi prompt tham chiếu đúng mã `FR-xx` liên quan — nếu Claude lệch khỏi FR đó, trỏ lại functional-requirements.md/studio-builder-spec.md/ai-integration-byok.md trong câu trả lời tiếp theo thay vì diễn giải lại từ đầu.
5. Mỗi prompt là 1 checkbox `- [ ]` — tick `- [x]` sau khi chạy xong và qua DoD, vẫn đọc từ trên xuống dưới theo đúng thứ tự phase. Prompt nào có ghi chú in đậm dạng "— đã có X, còn thiếu Y" nghĩa là **đã làm một phần thật** (không phải placeholder) — đọc kỹ phần "còn thiếu" trước khi chạy lại prompt đó để tránh làm lại từ đầu phần đã xong.

---

## Phase 0 — Nền móng

> **Trạng thái thực tế (kiểm tra trực tiếp trong repo):** mới xong phần **tooling/scaffold** — workspaces, catalogs, `turbo.json`, `.oxlintrc.json`/`.oxfmtrc.json` đều thật và `bun run lint/typecheck/build` chạy sạch; CI cơ bản đã có (`.github/workflows/ci.yml`). **Toàn bộ phần nghiệp vụ (mục 2-8 dưới đây) chưa bắt đầu** — `packages/{contracts,db,auth,drivers}/src/index.ts` và `apps/api/src/*.ts` vẫn là placeholder 2 dòng, `apps/dashboard/src` chỉ có scaffold Vite/shadcn mặc định (chưa router/auth/org switcher), chưa có file test nào. Đừng để checkbox đã tick ở mục 1 làm tưởng cả phase đã xong.

- [x] "Setup monorepo Turborepo + Bun workspaces theo đúng `.claude/rules/tech-stack.md` và `docs/architecture/architecture.md` §3: tạo cấu trúc `apps/`, `packages/` rỗng (chỉ package.json + tsconfig đúng preset), root `turbo.json`, `.oxlintrc.json`, `.oxfmtrc.json`, CI (lint/typecheck/test/build + remote cache). Việc này đụng nhiều package — thiết kế cấu trúc trước khi viết code." **— đã xong phần cấu trúc + CI cơ bản; còn thiếu remote cache (turbo remote caching chưa cấu hình, xem lại khi chạy prompt #9).**
- [x] "Tạo `packages/contracts`: zod schema cho toàn bộ entity ở `docs/architecture/database-schema.md` (org, user, membership, product, campaign, landing, lead, order, payment, ai_connection...). Chỉ schema + types, chưa cần DB thật."
- [x] "Tạo `packages/db`: Drizzle schema đúng `docs/architecture/database-schema.md`, migration, seed script, repository pattern — mọi hàm query bắt buộc nhận `orgId`, không có hàm 'query trần' (NFR-04, architecture.md §6). **Bắt buộc** implement helper `withOrgScope(orgId, fn)` gửi `SET LOCAL app.current_org` + query thật trong cùng transaction/batch (không phải 2 round-trip rời — RLS im lặng mất hiệu lực trên Neon serverless driver nếu tách rời, architecture.md §6.1); viết test tích hợp thật (2 org giả A/B, query chéo qua repository layer phải trả rỗng) — không chỉ test tầng app." **— bug nghiêm trọng đã tìm và sửa khi test Phase 1 sống: `apps/api/src/lib/db.ts`'s `createDbFromEnv` tạo **pool `postgres.js` mới hoàn toàn cho mỗi request** trên runtime Bun (đúng convention "built fresh per request" ghi trong code, nhưng convention đó chỉ đúng cho neon-http/CF — postgres.js là pool kết nối TCP thật, tạo mới mỗi request nghĩa là không bao giờ đóng, tự sập Postgres sau vài chục request thật). Đã sửa: Bun runtime giờ dùng module-level singleton (`bunDbSingleton ??= createPostgresDb(...)`), CF Workers vẫn tạo mới mỗi request (đúng vì neon-http không giữ connection). Nếu viết thêm code mới gọi `createDbFromEnv` ở Phase 2+, không cần tự cache lại — đã cache sẵn ở đây.**
- [x] "Tạo `packages/auth`: Better Auth + organization plugin + RBAC theo bảng quyền ở `docs/architecture/architecture.md` §6 (owner/admin/editor/sales) — FR-A-01 đến FR-A-04." **— bug đã sửa: `apps/api/src/lib/auth.ts`'s `createAuthFromEnv` luôn construct `Resend` client dù `RESEND_API_KEY` rỗng (crash ngay ở constructor) → signup 500 tuyệt đối ở local/dev không có key thật, trong khi `packages/auth/src/config.ts` đã viết sẵn nhánh optional-email "no-op khi không có `config.email`" nhưng chưa từng được dùng vì call site luôn truyền `email` object. Đã sửa: chỉ tạo sender + truyền `email` config khi có `RESEND_API_KEY`.**
- [x] "Tạo `packages/drivers`: interface `jobs`, `storage`, `cache`, `realtime`, `payments` (chỉ interface, SePay là impl cụ thể đầu tiên — VNPAY/MoMo/Casso/PayOS thêm sau không đổi interface, FR-D-10) (chưa cần impl VPS) + impl Cloudflare (QStash, R2, Upstash, SSE-hub, SePay) — đúng nguyên tắc portable ở architecture.md §1 nguyên tắc #2. Đây là boundary quan trọng nhất cho migration CF↔VPS sau này, thiết kế trước khi code." **— gap đã tìm khi test Phase 1 sống: `storage` chỉ có impl R2 (CF), Bun/VPS runtime không có driver nào (`lib/storage.ts` throw 501) → không cách nào chạy `bun run dev` để test Studio local (load HTML luôn 501). Đã thêm `packages/drivers/src/storage/local-fs.ts` — driver filesystem cho runtime Bun, **chỉ là hàng đứng tạm cho local dev** (giống vai trò docker-compose Postgres/Redis đứng tạm cho Neon/Upstash), **không phải** driver S3-compatible thật của Phase 7. Khi làm Phase 7 prompt #2 (đổi `storage` sang S3-compatible), thay driver này bằng driver S3 thật cho runtime Bun — đừng tưởng "storage" đã có driver VPS rồi mà bỏ qua prompt đó."
- [x] "Tạo `apps/api` skeleton Hono với 2 entrypoint `workers.ts` (CF) và `bun.ts` (VPS), error handling chuẩn, structured logging JSON (requestId, orgId — architecture.md §8), rate limit middleware."
- [x] "Tạo `apps/dashboard` skeleton: Vite 8 + React 19 + TanStack Router, layout cơ bản, auth flow (login/signup/verify email), org switcher, code-split theo route/module để đạt TTI < 3s lần đầu và chuyển route < 200ms (NFR-02), i18n scaffold tiếng Việt với kiến trúc message catalog ICU để thêm EN sau (NFR-07). Wiring Resend làm email provider cho Better Auth (verify email FR-A-01, invite FR-A-04, reset mật khẩu FR-I-02) — domain gửi đã chốt `mail.donve.vn`, địa chỉ `no-reply@mail.donve.vn` (FR-I-05/06); verify SPF/DKIM/DMARC trước khi test gửi thật."

**— đã xong: layout/router/i18n/org-switcher + `/api/auth/*` mount thật trong `apps/api`, email+password flow (login/signup/verify/forgot/reset) đủ dùng, `packages/drivers/src/email` (Resend, from mặc định `no-reply@mail.donve.vn`) wired vào `createAuth`. UI mời thành viên đã xong đầy đủ: `/members` (danh sách thành viên + lời mời đang chờ, mời/hủy/xóa qua Better Auth organization plugin thật, gate theo `manageMembers`), banner "lời mời đang chờ" cho người được mời (không cần accept-invite route riêng — họ thấy ngay sau khi đăng nhập), `sendInvitationEmail` wired trực tiếp vào `organization()` plugin (chạy nền, không chặn invite nếu Resend lỗi). Xóa `packages/auth/src/invites.ts`/`createInvite`/`acceptInvite` (hệ thống mời token-riêng cũ, chưa từng được mount ở đâu, chồng chéo với plugin) + cột `invites.token` (migration 0005) — chỉ còn một đường mời duy nhất qua organization plugin. Còn thiếu, cố ý để lại: nút Google/Facebook OAuth (chỉ email+password), ghi `email_logs`, và test gửi thật + verify SPF/DKIM/DMARC (cần `RESEND_API_KEY`/`DATABASE_URL` thật — không có trong môi trường này).**

- [x] "Viết cross-tenant test suite: user org A gọi mọi endpoint hiện có với id thuộc org B phải trả 404/403 (NFR-04, architecture.md §6) — viết test cho các endpoint auth/org vừa tạo, cả edge case." **— `apps/api/test/cross-tenant.integration.test.ts` (15 case, real Postgres qua testcontainers, chạy thật qua `createApp()`/Better Auth, không mock): get/update/delete/set-active org, list-members, invite-member, remove-member/update-member-role (cả 2 hướng IDOR: spoof orgId lẫn spoof member id trong org của chính mình), cancel/get/accept/reject-invitation, + 2 edge case (org id không tồn tại, request chưa đăng nhập). Phát hiện và sửa 3 bug chặn đứng toàn bộ mount `/api/auth/*` (chưa từng chạy thật): (1) bảng Better Auth (`user/session/account/verification`) chưa từng có migration; (2) `memberships` thiếu `createdAt`/`updatedAt`, `invites` thiếu `status`/`inviterId` mà organization plugin bắt buộc; (3) `packages/auth/src/permissions.ts`'s accessControl thiếu hẳn action `update` (org), `create/update/delete` (member), `invitation` — khiến mọi role kể cả owner bị từ chối mọi thao tác quản lý org/member/invite. Đã thêm migration 0002/0003 + sửa permissions.ts.**
- [ ] "Setup CI/CD theo infra-deployment-cost.md §6 (đã cập nhật §6 — `dev` đổi từ miniflare/wrangler dev+Neon branch sang **docker-compose local**, quyết định 2026-08-16: tối ưu chi phí, không phụ thuộc cloud khi code hàng ngày, `wrangler dev` chỉ dùng riêng lẻ khi cần test path CF Workers cụ thể): GitHub Actions chạy `turbo run lint typecheck test build` (affected-only qua remote cache), 3 environment `dev` (docker-compose Postgres+Redis local, `.env.local`, không cần secrets CI)/`staging` (Neon branch riêng)/`prod`, secrets qua Wrangler secrets tách riêng masterKey mã hoá BYOK (rotate được sau này); deploy staging tự động (CF Pages + Workers) cho `apps/dashboard` + `apps/api`. **Chưa mua VPS** (quyết định 2026-08-16, theo đúng khuyến nghị §3.3 — chờ Phase 5-6) nên prompt này không đụng Dokploy/VPS, chỉ 3 environment CF/Neon như trên."

**— code phần này đã xong: `.github/workflows/ci.yml` dùng `turbo run typecheck test build --affected` (fetch-depth 0 + `TURBO_TOKEN`/`TURBO_TEAM` optional cho remote cache free của Vercel — lint/fmt vẫn chạy full-repo, đúng `.claude/rules/tech-stack.md`); `.github/workflows/deploy-staging.yml` tự chạy sau khi CI xanh trên `main` (migrate Neon staging branch → sync Wrangler secrets → deploy `dv-api-staging` + CF Pages `dv-dashboard-staging`); `.github/workflows/deploy-prod.yml` là `workflow_dispatch` thủ công, gate qua GitHub Environment `prod` (required reviewers); `apps/api/wrangler.jsonc` có `env.staging`/`env.production` riêng. `docker-compose.yml` (root) + `README.md` local dev/verify cho `dev` đã có sẵn từ trước. **Còn thiếu, không thể làm qua code — xem `docs/runbooks/ci-cd-setup.md` mục 1-6**: tạo tài khoản Vercel remote cache, GitHub Environments `staging`/`prod` + toàn bộ secret (`TURBO_TOKEN`, `CLOUDFLARE_API_TOKEN`, `DATABASE_URL` staging/prod, v.v.), Neon staging branch, 2 CF Pages project, custom domain — chỉ tick `[x]` sau khi làm xong các bước đó và xác nhận 1 lần deploy staging thật thành công.**

**Trước khi qua Phase 1**: đối chiếu DoD — đăng ký → tạo org → mời member → phân quyền hoạt động; cross-tenant suite xanh; deploy staging tự động.

---

## Phase 1 — Studio core

> **Trạng thái thực tế (đã verify sống bằng Playwright, không chỉ đọc code):** mọi mục dưới đây đúng là đã implement thật (không phải placeholder), nhưng **chưa từng chạy end-to-end trước khi verify này** — toàn bộ `packages/studio-*`/`features/studio` còn **untracked, chưa commit lần nào**. Verify sống phát hiện và đã sửa 13 bug thật, phần lớn thuộc loại "code đúng ý định nhưng chưa ai bấm thử": route `/api/landings` 404 (Hono `mergePath` bỏ trailing slash khi mount root route — dashboard gọi `/api/landings/` không khớp route đăng ký thật `/api/landings`), route Studio không mở được (TanStack Router tự nest `landings.$id.studio` làm con của `landings.tsx` — không có `<Outlet/>` nên không bao giờ render, đã đổi `landings.tsx`→`landings.index.tsx` để router tự sinh layout trong suốt), org switcher không invalidate cache (đổi org không refetch gì, thấy data org cũ), Inspector nhập số thiếu unit (`setStyle(id,"width","400")` không có `px` → CSSOM âm thầm reject, hầu hết field Size/Box không có tác dụng), toạ độ overlay hover/select tính theo viewport thay vì container (`measureOverlayTarget` lệch hẳn ra ngoài canvas khi panel trái không sát viewport), **stale closure nghiêm trọng nhất**: mọi keydown trong iframe không bao giờ bubble ra `window` (2 browsing context riêng) → toàn bộ phím tắt V/E/C/D/Cmd+Z/Esc/Delete chết ngay sau lần click-chọn-element đầu tiên, kèm 1 bug con do cùng cơ chế (`handleSelect` đọc `mode` từ closure đông cứng lúc iframe load lần đầu, tự nhảy mode về "select" mỗi lần click), panel chat co lại ~45px (API `react-resizable-panels` v4 coi số nguyên là **pixel không phải %** — `defaultSize={22}` là 22px, phải truyền string `"22"`) + bug con tự lưu layout hỏng vào localStorage vĩnh viễn, Version History/"current" badge không cập nhật sau mỗi lần lưu (do cố ý không invalidate `landingKeys.detail` để tránh reload iframe — sửa bằng state local theo dõi riêng), thư mục `assets/` trong Design Files không click được (upload xong ảnh biến mất, không xem lại được). Toàn bộ 13 bug đã fix, verify lại sống, và `lint`/`typecheck`/`build`/test suite đều xanh sau mỗi lần sửa — xem chi tiết từng bug trong lịch sử hội thoại nếu cần đối chiếu, không lặp lại ở đây. Sau đó đã thêm 6 polish UX (chỉ báo autosave "Đã lưu HH:MM", tooltip phím tắt, tooltip "Sắp ra mắt" trên nút disabled, toast xác nhận comment, bỏ overlay dính khi qua Draw mode, bỏ cụm "Phase 2" khỏi copy hiển thị người dùng) — không đổi hành vi nghiệp vụ, chỉ đổi cảm nhận dùng.
>
> **Kết luận cho việc chạy tiếp Phase 2+:** không cần làm lại phần đã verify ở Phase 1 — nền đã vững cho đúng những luồng đã tự tay bấm thử (không chỉ đọc code): canvas/zoom-pan, hover/select, Edit inspector, LayerTree (hide/rename/reorder/delete+confirm), Comment mode, Draw mode, Design Files (upload/lightbox/version history), autosave, keyboard map. 2 điểm dưới đây trong Phase 2/6 gốc bị trùng với việc đã xong thật ở Phase 1, xem ghi chú tại đúng mục đó trước khi chạy để không làm lại. Ngoài ra: local dev giờ chạy được **toàn bộ** stack qua `bun run dev` (không cần `wrangler dev`/R2 thật) nhờ driver `local-fs` mới thêm ở Phase 0 — xem ghi chú ở prompt `packages/drivers`.
>
> **Chưa verify, vẫn cần làm trước khi tick DoD Phase 1 thật sự xong** (không nằm trong phạm vi lần review này): dropdown Export (HTML/ZIP/PNG) chưa bấm thử tay; test matrix Safari/Chrome/Firefox cho zoom/pan (yêu cầu tường minh của DoD) chưa chạy — Playwright ở review này chỉ chạy Chromium.

> Component dùng lại thay vì tự viết mới (đã có sẵn trong `packages/ui/src/components`): `shadcn/dialog`, `shadcn/select`, `shadcn/badge`, `shadcn/card`, `shadcn/toggle-group`, `shadcn/tabs`, `shadcn/resizable` (`react-resizable-panels`, đã trong catalog `ui`), `shadcn/scroll-area`, `shadcn/tooltip`, `shadcn/context-menu`, `shadcn/slider`, `shadcn/kbd`; AI Elements cho khung chat (`ai-elements/conversation`, `message`, `prompt-input`, `suggestion`, `queue`); icon từ `lucide-react`. Không tạo lại modal/dropdown/panel riêng nếu shadcn đã có primitive tương đương — chỉ style lại theo theme tối như screenshot.

- [x] "Implement `packages/studio-core` từ đầu (không port) — package thuần logic, **không React, không biết gì về CRM/dashboard** (architecture.md §1 nguyên tắc #3): (1) srcmap engine — sinh id ổn định (`cc-N`) cho mỗi element có ngữ nghĩa khi parse HTML, giữ map `id → node reference` cache để mọi patch là O(1) (studio-builder-spec.md §11); (2) patch ops đầy đủ: `replaceText`, `setStyle`, `setAttr`, `replaceOuterHTML`, `insertBefore`/`insertAfter`, `remove`, `toggleVisibility`, `renameLayer`, `moveBefore` — mỗi op là object thuần `{type, srcmapId, ...payload}`, áp được cả lên DOM thậts và lên chuỗi HTML (dùng khi server validate); (3) undo/redo stack ≥100 bước, mỗi entry là 1 op (không phải snapshot toàn file) để nhẹ; patch vào element đã bị `remove` trước đó phải no-op an toàn, không throw. Package độc lập, publish rõ type cho từng op để `studio-ai` (Phase 2) dùng lại làm patch protocol."
- [x] "Implement `packages/studio-ui` từ đầu — React hooks/components thuần UI, nhận `studio-core` làm engine, chưa biết gì về AI/chat (chat panel wiring ở prompt #4 dưới): mode system `view`/`select`/`edit`/`comment`/`draw` (context/state machine, 1 mode active tại 1 thời điểm); hook transform canvas `{scale, tx, ty}`; component `HoverSelectOverlay`, `LayerTreePanel`, `InspectorPanel` — style theo shadcn/Tailwind v4 (theme tối, khớp screenshot #1/#3), không hardcode màu ngoài token."
- [x] "Implement `packages/studio-ai` khung sườn (types + hàm rỗng có chữ ký đúng, **chưa gọi API thật** — Phase 2 nối logic): export type các patch ops đồng bộ với `studio-core` (dùng lại, không định nghĩa trùng), hàm `compilePrompt()` trả về base prompt tĩnh (constant), hàm `applyPatch()` gọi thẳng `studio-core`. Mục đích prompt này chỉ là dọn chỗ đứng đúng vị trí trong 4 lớp L0→L3 (architecture.md §3), tránh Phase 2 phải cấu trúc lại."
- [x] "Wiring `packages/studio-core`/`studio-ui`/`studio-ai` vào `apps/dashboard` như feature module L2 `features/studio` (architecture.md §3): React Query cho `landingPages` list/detail (`packages/contracts` schema đã có sẵn từ Phase 0), TanStack Router file-based route cho `/landings` và `/landings/$id/studio`."
- [x] "Implement route `/landings` — **trang đứng trước Studio editor, không vào thẳng canvas** (studio-builder-spec.md §2, FR-B-00): đúng bố cục Genspark ở screenshot #8 — trái: heading 'Bạn muốn tạo landing gì?' + `ai-elements/prompt-input` (textarea + `shadcn/select` chọn design system 'No design system' + nút gửi) + list 'popular tasks' gợi ý (icon + text + chevron, click điền vào prompt input); phải: lưới card landing dạng `shadcn/card` (thumbnail 16:9 từ `.thumbnail.jpg`, tên, `shadcn/badge` Draft/Published — suy từ `deployments.status=live` có tồn tại hay không, campaign gắn, 'cập nhật X trước'). Filter bằng `shadcn/toggle-group` (Tất cả/Published/Draft) + `shadcn/select` theo campaign + search input debounce theo tên + sort theo cập nhật gần nhất/tên (FR-B-00b). Grid ảo hoá bằng `@tanstack/react-virtual` (đã có trong catalog `data`) khi > 30 card (studio-builder-spec.md §2). Submit prompt → tạo `landingPages` rỗng + điều hướng thẳng `/landings/$id/studio` ở trạng thái đang generate — **không có màn hình trung gian 'đang tạo' riêng**, gọi AI generate thật nối ở Phase 2 (FR-B-21), Phase 1 chỉ cần tạo record + route đi kèm state 'pending' hiển thị skeleton trong canvas (FR-B-00c). Card actions qua `shadcn/dropdown-menu` (nút ⋯): đổi tên, nhân bản, xoá (soft-delete), xem live nếu published, gỡ khỏi campaign (FR-B-00d, P1). Empty state org mới: chỉ prompt bar ở giữa màn hình, không hiện grid."
- [x] "Implement layout tổng thể Studio đúng studio-builder-spec.md §3 + screenshot #1/#2/#6: TopBar cố định trên cùng (nút back, nút toggle sidebar tooltip 'Show chat'/'Hide chat' đúng vị trí screenshot #6, tên project inline-editable với icon ✎, `Share`/`New`/`⋯` phải); dưới TopBar chia 2 vùng qua `shadcn/resizable` (splitter kéo được, lưu `panelSizes` localStorage theo user — không theo landing page cụ thể): trái ChatPanel thu gọn/mở rộng được (FR-B-01/02, khi collapse canvas chiếm full width); phải WorkArea gồm `shadcn/tabs` `Design Files` / `<Page>.html` (tên tab = tên file HTML thật, không phải chữ tĩnh), dưới tab là toolbar canvas: nút refresh (⟳), `+ Tweaks`, `Comment`, `Edit`, `Draw`, zoom indicator (`75%`) — và tách biệt trên TopBar còn `Export` (`shadcn/dropdown-menu`: HTML/ZIP/PNG), `Present` (mở fullscreen preview route riêng hoặc overlay), `Download`, `Share` (FR-B-03). State `chatCollapsed`/`activeTab` cũng persist localStorage per user, không reset khi F5. Đây là khung UI toàn bộ studio — dựng xong layout rỗng (không cần canvas engine thật) trước, verify đúng pixel/vị trí so với screenshot rồi mới sang prompt canvas."
- [x] "Implement canvas: `<iframe sandbox=\"allow-same-origin\">` (**không** thêm `allow-scripts` — HTML landing không cần chạy JS lúc thiết kế, runtime script chỉ inject lúc publish ở Phase 3; đây là quyết định bảo mật, không phải thiếu sót) nạp nội dung qua `srcdoc` từ version hiện hành, mọi patch áp trực tiếp vào `iframe.contentDocument` — không reload trừ khi restore version (FR-B-04). Zoom 10%-400% quanh vị trí con trỏ: touchpad pinch = `wheel` với `e.ctrlKey===true` (`scale' = clamp(scale * exp(-deltaY*0.01), 0.1, 4)`), `Cmd +`/`Cmd -`/nút UI, chuột thường = `Ctrl/Cmd+wheel` (FR-B-05); pan = space-drag (cursor grab/grabbing), two-finger scroll không ctrl, middle-mouse drag; Safari cần xử lý riêng `gesturestart`/`gesturechange` cho pinch mượt trên trackpad Mac (studio-builder-spec.md §4.2). Fit-to-screen mặc định khi mở trang + double-click nền canvas reset lại fit (FR-B-07). Canvas mở rộng qua splitter chat/canvas đã làm ở prompt trên, không làm lại. Transform dùng `transform: translate(tx,ty) scale(s)` + `transform-origin:0 0`, mọi update trong `requestAnimationFrame`, `will-change:transform` (FR-B-06). **Bắt buộc test matrix Safari/Chrome/Firefox cho cả touchpad và chuột trước khi tick DoD** — đây là rủi ro kỹ thuật đã biết (implementation-plan.md bảng rủi ro 'Zoom/pan lệch toạ độ đa trình duyệt', xác suất Trung) và **không có code cũ để tham khảo cách đã né lỗi này** — tự viết test case, đừng giả định trình duyệt nào cũng giống nhau."
- [x] "Implement overlay hover/select đặt ở layer div **ngoài** iframe (không chèn DOM vào trang), toạ độ đồng bộ qua `getBoundingClientRect` của element trong iframe nhân với transform canvas hiện tại (studio-builder-spec.md §4.1, lý do: không làm bẩn DOM trang gốc): ở mode `comment`/`edit`, hover element có srcmap id → border 1.5px nét đứt màu primary + label chip `{tag} [{srcmapId}] \"{text rút gọn 20 ký tự}\"` (đúng mẫu `span [cc-2] \"STREET\"` ở screenshot #4/#6, dùng `shadcn/badge` variant tối làm chip); click → border 2px nét liền, label giữ, đồng bộ scroll+highlight LayerTree (FR-B-08/09). Debounce hover 16ms (~1 frame); bỏ qua `<html>`/`<body>`; hit-test phải nhân nghịch đảo transform, không lệch khi đã zoom/pan."
- [x] "Implement Comment mode (FR-B-12/13, screenshot #5): click element đang hover dashed → mở `shadcn/dialog` đúng bố cục ảnh — textarea placeholder 'Describe the issue or suggestion...' (`shadcn/textarea`), nút đính ảnh góc trái dưới (P1, để icon sẵn chưa cần wiring upload), 2 nút phải: `Queue` (`shadcn/button` variant outline) và `Send to Chat` (variant chính). Payload lưu `{srcmapId, selectorPath, elementScreenshot, body}` vào bảng `studioComments` (database-schema.md, status mặc định `queued` nếu bấm Queue). `Send to Chat` (Phase 1 chỉ cần tạo `chatMessages` row + chip UI — AI thực sự trả lời nối ở Phase 2): hiển thị trong ChatPanel dạng `ai-elements/message` kèm chip 'Commented on element' y hệt screenshot #1 (badge nhỏ + icon comment). Queue: badge đếm số lượng trên nút mở queue (dùng `ai-elements/queue` nếu component đó đúng shape, không thì `shadcn/badge` trên icon), nút 'Send all' gộp toàn bộ comment `queued` thành 1 message duy nhất dạng liệt kê `#1 cc-2 'STREET': <body>`, `#2 ...` rồi set các comment đó `sent`. Comment của element đã bị xoá (op `remove`) hiển thị mờ/gạch ngang trong queue, không xoá khỏi DB (orphan, vẫn audit được)."
- [x] "Implement Draw mode (FR-B-14, P1): toolbar riêng khi bật Draw (bút, mũi tên, khung chữ nhật, chọn màu, undo nét vừa vẽ, clear toàn bộ) vẽ lên `<canvas>` overlay khớp transform canvas hiện tại. Nút gửi: composite ảnh preview hiện tại + nét vẽ đè lên (client-side canvas compositing) → đính vào ChatPanel kèm text người dùng nhập, prompt mặc định đính kèm khi không nhập gì: 'Apply the marked-up changes to <Page>.html. The attached image shows the current preview with my annotations drawn on top.' (đúng nguyên văn ở screenshot #1)."
- [x] "Implement Edit mode inspector (FR-B-10, screenshot #3): panel trái xuất hiện khi chọn element ở mode Edit, header 'EDIT' + nút X đóng, 3 nhóm field đúng thứ tự và đúng tên trong ảnh — **TYPOGRAPHY**: Font (dropdown), Size + Weight (2 cột), Color (color swatch + hex text input) + Align (dropdown), Case (dropdown: none/uppercase/lowercase/capitalize) + Style (dropdown: normal/italic), Decoration (dropdown) + Line (number, line-height), Tracking (number, letter-spacing, px); **SIZE**: Width, Height; **BOX**: Opacity, Overflow (dropdown), Padding, Margin, Border + BColor (color), Radius. Dùng `shadcn/native-select` cho dropdown, `shadcn/input`/`shadcn/input-group` cho field số có suffix `px`. Mỗi field commit (blur/Enter, không phải mỗi keystroke) → op `setStyle(srcmapId, prop, value)` → áp inline style vào iframe ngay (không reload) + đẩy undo stack + debounce 800ms tạo `pageVersions` mới origin=`manual` (FR-B-10, studio-builder-spec.md §5). Style ghi vào inline `style` attribute của element trong HTML — v1 không dùng class utility Tailwind cho content AI/manual sinh ra, chỉ dashboard code tự viết mới dùng Tailwind. Chuẩn hoá input số kiểu vi-VN (`0,86` / `−0,16`) thành dấu chấm khi ghi CSS thật, hiển thị lại theo locale khi đọc ra. Inline text edit: double-click text element (chỉ cho phép khi element chỉ chứa text node/inline đơn giản, chặn nếu có element con phức tạp) → `contenteditable` tại chỗ, auto-select hết nội dung, Enter hoặc blur = commit op `replaceText`, Esc = huỷ không ghi (FR-B-11)."
- [x] "Implement LayerTree panel (FR-B-16/17, screenshot #1): header 'LAYERS' + đếm tổng (badge) + chevron thu gọn cả panel; mỗi item: icon 👁 toggle ẩn/hiện (op `toggleVisibility`, ẩn thật `display:none` + cờ srcmap `hidden:true`), icon loại (`T` cho text, thumbnail ảnh thật cho image layer — thấy rõ khác biệt ở item cuối 'Satay grill (background)' trong ảnh), tên layer (do AI đặt tên ngữ nghĩa khi generate — Phase 2 nối, Phase 1 fallback dùng tag+index nếu chưa có tên AI). Click item = select element trên canvas + scroll-into-view; hover item = highlight nét đứt trên canvas (đồng bộ ngược overlay ở prompt trên). Footer hint tĩnh dịch tiếng Việt: 'Trên cùng danh sách = layer nằm trên cùng. Click biểu tượng mắt để ẩn.' Rename layer double-click hoặc icon ✎ hiện khi hover (FR-B-18, P1) → input inline, Enter/blur commit vào srcmap metadata (op `renameLayer`). Kéo thả đổi thứ tự trong cùng parent (FR-B-19, P1, op `moveBefore`) — dùng **dnd-kit** (`@dnd-kit/core` + `@dnd-kit/sortable`, xem `tech-stack.md` — cùng lib sẽ dùng lại cho kanban CRM ở FR-E-02 Phase 4, không phải 2 lib kéo-thả khác nhau trong dashboard); **`@dnd-kit/core`/`@dnd-kit/sortable` chưa có trong root catalog** — thêm vào catalog `ui` ở root `package.json` trước khi `bun add` trong `packages/ui` hoặc `apps/dashboard`, đúng `.claude/rules/tech-stack.md` (không hardcode version trong workspace package.json). LayerTree ảo hoá bằng `@tanstack/react-virtual` khi > 100 item (studio-builder-spec.md §11)."
- [x] "Implement Undo/Redo toàn cục cho mọi thay đổi source (manual edit, inline text edit — AI patch nối ở Phase 2 dùng lại đúng cùng cơ chế undo stack này, không viết stack riêng), `Cmd+Z`/`Shift+Cmd+Z`, ≥100 bước trong session, đọc trực tiếp undo stack đã implement ở `packages/studio-core` — không tạo stack thứ 2 ở tầng UI (FR-B-15). Đầy đủ keyboard map studio-builder-spec.md §10: `V`/`E`/`C`/`D` đổi mode view/edit/comment/draw, `Cmd+=`/`Cmd+-`/`Cmd+0`/`Cmd+1` zoom in/out/100%/fit, `Space+drag` pan, `Esc` bỏ chọn/đóng modal, `Delete` xoá element chọn (op `remove`, `shadcn/alert-dialog` confirm khi element là section lớn — ngưỡng: có ≥5 element con hoặc chiếm >30% chiều cao trang), `Cmd+S` force save version ngay không chờ debounce 800ms. Toàn bộ shortcut phải tắt khi focus đang ở input/textarea/contenteditable (trừ Esc) — tránh chặn gõ chữ thường. Dashboard phải điều hướng được hoàn toàn bằng bàn phím ở studio, focus ring rõ (NFR-08)."
- [x] "Implement tab Design Files (FR-B-26, screenshot #2): cây cố định 4 nhóm **FOLDERS** (`assets/`, `screenshots/` — icon folder), **PAGES** (`<Page>.html` — icon globe/browser, click mở tab preview tương ứng ở WorkArea), **DATA** (`<Page>.html.srcmap.json` — icon `{}`, click mở viewer JSON read-only), **IMAGES** (`.thumbnail.jpg` — icon ảnh, click mở lightbox). Header 'Project' + nút `+` (upload nhanh) + refresh. Upload ảnh vào `assets/` qua drag-drop hoặc nút `+`: nén + chuyển WebP/AVIF, ghi `pageAssets` (FR-B-29) với `source='user_upload'`. Version history (FR-B-27): list version theo `pageVersions.seq` giảm dần, mỗi item hiện thời điểm + origin (`manual`/`import`/`restore` — `ai_patch`/`ai_full` nối Phase 2) + nhãn nếu có; click 2 version = mở diff (2 khung iframe preview cạnh nhau + text diff HTML, dùng lại `shiki`/`streamdown` đã có trong catalog `ui` nếu phù hợp cho code diff, không thêm lib diff mới); nút Restore (tạo version mới origin=`restore` copy nội dung, không xoá lịch sử); nút đặt nhãn (text input ngắn) lưu vào `pageVersions.label`. Thumbnail `.thumbnail.jpg`: tự chụp bằng `modern-screenshot` (đã có trong catalog `ui`) ngay sau mỗi version mới được tạo (kể cả manual edit debounce, restore, không chỉ AI) — chạy ở client, upload kết quả lên R2 qua endpoint có sẵn từ `packages/drivers` storage (Phase 0)."
- [x] "Implement Export (FR-B-28): dropdown `Export` ở TopBar (đã dựng khung ở prompt layout, nối logic thật ở đây) — HTML: xuất đúng file `<Page>.html` hiện hành (đơn file, đã inline CSS từ trước, không cần build gì thêm); ZIP: HTML + toàn bộ `assets/` liên quan (dùng lib zip đã/sẽ có trong catalog `content`, kiểm tra trước khi thêm mới — không trùng lib với `sanitize-html`/`dompurify` đã có, đây là nhu cầu khác); PNG full-page: chụp qua `modern-screenshot` client-side, full chiều cao trang (không cắt theo viewport). Cả 3 định dạng trigger download qua Blob URL, không round-trip server nếu không cần (asset ảnh có thể cần fetch từ R2 signed URL để gộp vào ZIP)."
- [x] "Viết test cho toàn bộ patch ops của `packages/studio-core` (`replaceText`, `setStyle`, `setAttr`, `replaceOuterHTML`, `insertBefore`/`insertAfter`, `remove`, `toggleVisibility`, `renameLayer`, `moveBefore`) — đảm bảo undo/redo đúng cho từng loại (undo phải trả đúng giá trị trước đó, không phải giá trị rỗng/mặc định), cả edge case: patch vào element đã bị `remove` trước đó (không throw, no-op có log), 2 patch chồng lấn lên cùng srcmap id liên tiếp (undo lần lượt phải lùi đúng thứ tự, không nhảy cóc), `moveBefore` giữa 2 phần tử không cùng parent (phải reject, không silent-fail), undo sau khi đã vượt quá 100 bước (entry cũ nhất bị drop, không lỗi). Vì không có code cũ để đối chiếu, test phải tự đứng độc lập chứng minh đúng — không viết test khớp theo hành vi hiện tại của code chưa được review."

**DoD Phase 1**: mở 1 file HTML seed thật (không phải mock rời), thao tác đầy đủ đúng luồng ở screenshot #1 (chat + canvas + layers) / #2 (Design Files) / #3 (Edit inspector) / #4 và #6 (hover/select/comment) mà không lệch bố cục/label so với ảnh; version history list/diff/restore chạy đúng; toàn bộ keyboard map ở studio-builder-spec.md §10 hoạt động; export HTML/ZIP/PNG ra đúng file mở lại được; test matrix Safari/Chrome/Firefox cho zoom/pan đã chạy và ghi kết quả (không chỉ "chạy được trên Chrome dev"); test patch ops + undo/redo xanh.

---

## Phase 2 — AI layer

> Đường găng của cả dự án (implementation-plan.md) — đừng rút gọn test bench, đây là phase quyết định AI patch có tin cậy được không.

- [x] "Tạo `packages/ai-gateway`: provider abstraction **OpenRouter (mặc định) + Anthropic + OpenAI** qua AI SDK v6, key vault mã hoá AES-256-GCM (NFR-05), usage metering theo token. Đây là boundary bảo mật quan trọng — review kỹ phần lưu/giải mã key."
- [x] "Implement BYOK UI trong dashboard: connect API key (mặc định gợi ý OpenRouter trước — key duy nhất, có model free để test), validate bằng call thử, chọn model mặc định (FR-H-01); routing trong ai-gateway — nếu `connectionId=platform` thì trừ `aiCreditBalance` **atomic** trong cùng transaction với insert `aiUsage` (`UPDATE ... WHERE ai_credit_balance >= cost`, database-schema.md ghi chú #8 — không check-rồi-trừ 2 bước riêng, race condition khi nhiều request đồng thời), hiển thị usage cho tenant (FR-H-02, gói trả phí); chọn model mạnh cho generate lần đầu, model rẻ (DeepSeek qua OpenRouter/Haiku) cho patch nhỏ/đặt tên layer để tiết kiệm chi phí (ai-integration-byok.md §2, §6). Implement thêm N lần dùng thử (vd 3) không cần BYOK, chạy trên key nền tảng qua Cloudflare Workers AI, không phải Gemini (FR-H-05, ai-integration-byok.md §6) — giảm ma sát onboarding non-tech."
- [x] "Implement Chat UI bằng AI SDK v6 `useChat` + AI Elements (Conversation, Message, PromptInput, Reasoning): streaming, đính ảnh, suggestion chips (FR-B-20)." **— đã có 1 phần thật ở Phase 1, không viết lại từ đầu: `chat-panel.tsx` đã render list `chatMessages` thật (text/image/comment-context chip) qua React Query, có empty state — code tự ghi rõ trong comment "chat history/prompt-input/AI Elements wiring is its own prompt (Phase 2)". Còn thiếu đúng như tên prompt: `useChat`/streaming/`PromptInput` để **gửi** được tin, không phải phần hiển thị.**
- [x] "Implement prompt compiler trong `packages/studio-ai`: base prompt + skills đang bật + design tokens + srcmap context + comment queue → system prompt cuối cùng (architecture.md §5.1, FR-B-24)."
- [x] "Implement tool call `apply_patch` end-to-end theo schema ai-integration-byok.md §4: server validate ops bằng studio-core → áp lên HTML hiện tại → tạo `page_version` mới → trả patch cho client; client áp cùng patch vào DOM iframe optimistic (không reload) + đẩy undo stack (FR-B-22/23). Fallback full-file khi patch validate fail." **— cơ chế client-side cần dùng lại đã có và đã hardening ở Phase 1: `canvas.tsx`'s `commit`/`historyControlsRef` (undo stack thật, không viết stack thứ 2) + `saveQueueRef` (serialize các lần lưu, tránh race undo/redo dồn dập ghi đè sai `currentVersionId` — bug thật đã gặp và sửa khi test sống). AI patch chỉ cần gọi đúng `commitRef.current(op)` như Inspector/LayerTree đang làm, không tự viết đường ghi riêng."
- [x] "Bảo vệ prompt injection: nội dung trang import/AI đọc phải bọc trong delimiter rõ ràng, system prompt chỉ định đó là data không phải lệnh (architecture.md §7) — review lại đoạn xử lý này, đây là input không tin cậy đi thẳng vào AI."
- [x] "Implement generate lần đầu: prompt → AI sinh HTML single-file inline CSS tuân theo skills đang bật → hiện canvas + tự tạo srcmap + layer tree (FR-B-21). Nguồn ảnh minh hoạ AI chèn: ưu tiên tuyệt đối ảnh tenant đã upload, chỉ dùng Unsplash/Pexels API (license thương mại, ghi `pageAssets.source/license`) khi tenant chưa có ảnh phù hợp, hỏi trước khi tự chèn stock (FR-B-32/33) — không tự sinh ảnh AI ở v1 (FR-B-34)." **— checkbox này trước đó bị tick sai (không có ghi chú, và code thật không tồn tại — đã verify bằng cách đọc trực tiếp repo trước khi làm): chưa từng có route `/generate`, chưa có `compileGeneratePrompt`, chưa có `skillsRepository`, chưa có tích hợp Unsplash/Pexels nào. Đã implement thật từ đầu: `POST /api/landings/:id/generate` (gọi AI 1 lần không cần tool-calling — prompt yêu cầu trả thẳng HTML thô, không dùng `apply_full_html` vì route này không stream/không có tool loop), `compileGeneratePrompt` (`packages/studio-ai`, tách riêng khỏi `compilePrompt` vì không có srcmap/comment context, có thêm rule ảnh FR-B-32/33/34), `stampSrcmap`/`srcmapToJson` (`packages/studio-core`, để tạo cả `htmlKey` lẫn `srcmapKey` cho version đầu tiên — trước đó không ai từng tạo srcmap.json thật, `chat/stream`'s `persistVersion` chỉ copy `srcmapKey` cũ), `skillsRepository.listEnabledForLandingPage` (bảng `skills`/`landingSkills` đã có, chưa từng có CRUD). Ảnh FR-B-32/33: AI được yêu cầu chèn placeholder `data-cc-need-image="mô tả"` thay vì tự bịa URL; sau khi generate xong, nếu còn placeholder thì tự động gửi 1 tin nhắn chat (role assistant, text thường — không thêm loại `ChatContentPart` mới) hỏi thẳng đúng câu spec yêu cầu; chỉ khi tenant xác nhận mới chèn ảnh thật qua 2 endpoint mới `POST /api/studio/images/suggest` (Unsplash trước, Pexels fallback, cả 2 optional qua `UNSPLASH_ACCESS_KEY`/`PEXELS_API_KEY`, trả `[]` nếu chưa cấu hình) và `POST /api/studio/images/apply` (tải ảnh về lưu R2 thật — không hotlink — ghi `pageAssets.source=stock_licensed` + `license`). Frontend: prompt bar giờ truyền nguyên văn prompt qua search param `?prompt=` khi điều hướng sang `/landings/$id/studio` (trước đó prompt gốc bị cắt còn 120 ký tự làm `name` rồi mất luôn); trang studio tự gọi `/generate` một lần khi thấy `prompt` (guard bằng ref, không phải chỉ check giá trị — tránh gọi 2 lần do re-render), xoá `prompt` khỏi URL khi thành công, có nút Retry khi lỗi. **Việc thật còn thiếu, cố ý để lại**: chưa có UI hiển thị danh sách ảnh gợi ý (carousel/picker) trong ChatPanel — 2 endpoint suggest/apply đã hoạt động đầy đủ và có thể gọi thẳng, nhưng cần 1 component UI riêng để tenant bấm chọn ảnh thay vì gõ lệnh; sanitize HTML AI trả về (chặn `<script>`/URL độc hại) chưa có — cùng loại rủi ro với phần import HTML đã ghi nhận ở nơi khác, chưa xử lý ở generate. Nhân tiện sửa 1 bug có sẵn không liên quan trực tiếp nhưng chặn đứng việc verify: `apps/dashboard/package.json`'s `typecheck` script là `tsc --noEmit` chạy trên `tsconfig.json` kiểu solution (`"files": []` + `references`) — không có `-b` nghĩa là **không check gì cả, luôn exit 0 dù code sai** (đã tận mắt thấy `tsc -b` bắt được 2 lỗi thật mà `tsc --noEmit` bỏ qua hoàn toàn khi làm prompt này). Đã sửa thành `tsc -b` — `noEmit: true` đã có sẵn trong `@dv/config/vite-app.json` nên không phát sinh file build lạ.**
- [x] ~~"Implement Comment mode: click element → modal (textarea, đính ảnh P1) → Queue hoặc Send to Chat; comment gửi = message kèm ngữ cảnh element (srcmap id, ảnh crop, selector), hiển thị chip 'Commented on element' trong chat (FR-B-12); queue có badge đếm + nút 'Send all' gộp 1 request AI (FR-B-13)."~~ **— XOÁ khỏi Phase 2, đã xong 100% ở Phase 1 (không phải UI rỗng): `comment-dialog.tsx` (modal/Queue/Send to Chat), `comment-queue.tsx` (badge đếm/Send all), `chat-panel.tsx` (chip). Đã verify sống qua Playwright: queue → toast, send → tạo `chatMessages` với `comment-context` part đúng payload. Việc thật còn lại cho Phase 2 không phải viết lại prompt này. **Đã kiểm tra lại: prompt apply_patch (#88 phía trên) đã tick `[x]` xong rồi và không hề xử lý `comment-context`** — `compilePrompt()` (`packages/studio-ai/prompt.ts`) chỉ đưa comment vào "Comment queue" của system prompt khi được truyền tay qua tham số `comments`, không tự đọc `comment-context` part từ lịch sử message; text part hiện có (`comment.body`) không kèm `srcmapId` nên AI không biết sửa đúng phần tử nào. Vậy đây là 1 prompt mới thật sự, chưa nằm trong phạm vi việc đã chạy: "Khi convert `chatMessages` sang model messages, expand mỗi `comment-context` part thành context có `srcmapId` + `body` (join với `studioComments` nếu cần) trước khi gọi `compilePrompt`/model, để AI biết chính xác phần tử cần patch." **Đã implement**: `send-all` đã tự embed srcmapId vào text (`#N [srcmapId]: body`) nên không cần sửa; gap thật chỉ ở path "Send to Chat" đơn lẻ — `data-comment-context` là data-part nên `convertToModelMessages` bỏ qua, model không thấy srcmapId. Sửa ở `routes.ts`'s `/chat/stream`: hàm `expandCommentContextForModel()` quét `data-comment-context` part trong mỗi message rồi append 1 text part `(Referring to element: <srcmapId>)` **chỉ cho lượt gọi model** (không đụng vào `comment.body` lưu DB hay text hiển thị trong ChatPanel, tránh lặp với badge "Commented on element" đã render sẵn ở UI). `typecheck`/`lint` xanh.
- [x] "Tạo `packages/db` migration + CRUD cho Skill (FR-F-01) và Prompt template (FR-F-03, biến `{{brand}}`/`{{product}}`/`{{tone}}`, versioning, preview compiled prompt). Viết nội dung 4 skill nền tảng: `seo-landing-vn`, `cwv-budget`, `copywriting-chuyen-doi`, `form-phễu-chuẩn` (đây là moat theo implementation-plan.md — nội dung do bạn viết, Claude chỉ dựng CRUD/UI, không tự bịa nội dung dạy học)." **— checkbox này trước đó bị tick sai giống #90 (không có ghi chú, và CRUD thật không tồn tại — đã verify bằng cách đọc trực tiếp repo trước khi làm): schema `skills`/`prompt_templates` đã có sẵn từ trước (dựng incidental lúc làm FR-B-21 generate ở #90) nhưng chỉ có đọc (`skillsRepository.list`/`listEnabledForLandingPage`), chưa từng có insert/update/remove cho `skills`, chưa có repository nào cho `promptTemplates`, chưa có route API nào cho 2 entity này, chưa có logic compile `{{brand}}`/`{{product}}`/`{{tone}}`. Đã implement thật: migration `0006_add_prompt_template_versioning.sql` (thêm `updated_at` + unique index `(org_id, slug)` cho `prompt_templates` để khớp với `skills`, sửa default jsonb của `sections`/`variables` từ `{}` sang `[]` cho đúng shape mảng mà `packages/contracts` đã định nghĩa từ trước); `skillsRepository.insert/update/remove` (write bị khoá `eq(orgId, orgId)`, không dùng `ownedBy` như đọc — tenant không bao giờ sửa/xoá được skill platform `orgId=null` qua route); `promptTemplatesRepository` mới (`packages/db/src/repositories/prompt-templates.ts`) cùng pattern, `update` tự tăng `version` bằng `sql\`version + 1\``mỗi lần sửa (versioning FR-F-03) — không cần client tự tính;`compilePromptTemplate()`(pure function, ghép nội dung mọi section rồi thay`{{key}}`bằng giá trị tương ứng, biến thiếu giữ nguyên literal`{{key}}`thay vì âm thầm bỏ trống); contracts mới`createSkillSchema`/`updateSkillSchema`/`createPromptTemplateSchema`/`updatePromptTemplateSchema`/`compilePromptTemplateSchema`/`compiledPromptSchema`; route mới trong `apps/api/src/modules/ai/routes.ts`: `GET/POST/PATCH/DELETE /api/ai/skills(/:id)`, `GET/POST/PATCH/DELETE /api/ai/prompt-templates(/:id)`, `POST /api/ai/prompt-templates/:id/compile` (preview, không persist). **Cố ý chưa làm — đúng như prompt tự ghi rõ**: nội dung thật của 4 skill (`seo-landing-vn`/`cwv-budget`/`copywriting-chuyen-doi`/`form-phễu-chuẩn`, tức FR-F-02) — đây là 1 mục P0 riêng trong functional-requirements.md#144, nội dung do bạn viết, không phải phần CRUD/migration của prompt này; UI editor (textarea + `streamdown`preview split-pane theo tech-stack.md) cũng chưa có, chỉ mới có API.`typecheck`/`lint`xanh (đã chạy`bun run typecheck`+`bun run lint` toàn repo).**
- [x] "Test bench prompt (FR-F-04, P1): chạy 1 prompt với model chọn, chạy Lighthouse sandbox trên output, so sánh 2 version — cần cho eval set 20 trang mẫu regression mỗi khi đổi prompt (implementation-plan.md đối sách rủi ro AI patch)." **— checkbox này trước đó cũng bị tick sai như #90/#92 (không có code thật, đã verify bằng cách đọc trực tiếp repo trước khi làm — không có bảng eval/test-bench/lighthouse nào, không route nào, chỉ có field `deploymentMetaSchema.lighthouseScore` không liên quan — đó là Lighthouse CI publish-time (NFR-01), khác hẳn test bench này). Đã implement thật: bảng `prompt_test_runs` mới (migration 0007, RLS `orgIsolationPolicy` giống mọi bảng nhạy cảm khác) lưu mỗi lần chạy (model, compiled prompt, output HTML, điểm Lighthouse, usage); `promptTestRunsRepository.insert/listByTemplate`; route `POST /api/ai/prompt-templates/:id/test-run` (compile → gọi model qua đúng cơ chế routing trial/platform/BYOK đã có ở `/generate` — tách logic đó ra `runModelCompletion()` dùng chung, không copy 100 dòng branching billing 2 lần — `/generate` giờ gọi lại đúng hàm này, hành vi không đổi) + `GET /api/ai/prompt-templates/:id/test-runs` (list cho UI so sánh 2 version chọn từ đây). Lighthouse sandbox (`lib/lighthouse-sandbox.ts`): `Bun.serve` phục vụ HTML tại port ngẫu nhiên, `chrome-launcher` mở Chrome headless thật, `lighthouse` chấm 4 category (performance/accessibility/best-practices/seo) — **chỉ chạy được trên runtime Bun** (kiểm `env.RUNTIME`), trả `null` trên CF Workers vì không có process thật để spawn Chrome (model call vẫn thành công, chỉ thiếu điểm). Đã tận mắt xác nhận bằng `wrangler deploy --dry-run`: import thẳng `"lighthouse"`/`"chrome-launcher"` bằng string literal làm bundle Workers phình từ ~1MB lên **31.5MB** (gzip 4.9MB, sát giới hạn plan trả phí) dù 2 lib này không bao giờ chạy được ở đó — sửa bằng specifier biến (`const m = "lighthouse"; import(m)`) để esbuild không thể bundle tĩnh, đưa bundle về đúng ~5.7MB (gzip 1MB), khớp con số trước khi thêm feature này. **Cố ý chưa làm**: UI test-bench (nút "Chạy thử" + hiển thị output/điểm Lighthouse + view so sánh 2 version) trong prompt-template editor — 2 endpoint đã hoạt động đầy đủ và gọi thẳng được, chỉ thiếu component; corpus thật 20 trang mẫu + job CI tự chạy regression mỗi khi đổi prompt (đây là phần "20 trang mẫu" nói trong implementation-plan.md — cần nội dung landing mẫu thật do người viết chọn, không phải thứ Claude tự bịa). `typecheck`/`lint` xanh toàn repo.**

**DoD Phase 2**: prompt → landing hoàn chỉnh có srcmap + layer đặt tên; comment 1 element → AI sửa đúng element; đổi model/provider giữa chừng OK.

---

## Phase 3 — Publishing

- [x] "Tạo `apps/edge-router` CF Worker: KV hostname→deployment_id, R2 deployments/<id>/*, Cache API immutable, event beacon `/e/*` (architecture.md §2). Asset tĩnh đặt tên theo content-hash, `Cache-Control: public, max-age=31536000, immutable`; HTML gốc `no-store`/ngắn (xem §5.2 cache invalidation) (NFR-14). Rate limit `/e/*` theo IP + campaign, cộng thêm cache 2s edge đã có (NFR-16)."
- [x] "Implement build pipeline publish theo **outbox pattern** (architecture.md §5.2 — đã đổi từ KV-put-trực-tiếp): tạo `deployments` status=building → sanitize → minify html/css → hash assets → rewrite URL → inject runtime script defer + meta/OG/JSON-LD + canonical + beacon → upload R2 → tạo `publish_outbox` row status=pending → worker áp KV put → KV xác nhận thành công thì set `deployments.status=live` VÀ `publish_outbox.status=applied` trong cùng bước → warm cache + chụp thumbnail (FR-G-01). Thêm partial unique index `deployments(hostname) WHERE status='live'` (database-schema.md) và job reconciliation định kỳ so KV thật với Postgres."
- [x] "Tạo `apps/landing-runtime` v1 (vanilla TS ~5-8KB, build IIFE qua tsdown — package compiled duy nhất, đúng ngoại lệ ở `.claude/rules/tech-stack.md`): chỉ form + popup đăng ký, chưa cần payment (FR-D-01 rút gọn cho phase này)."
- [x] "Implement Publish/Rollback/Unpublish UI trong dashboard: rollback đi qua **cùng cơ chế outbox** ở prompt #2 (tạo outbox row mới trỏ `targetDeployId` về deployment cũ), không phải KV put trực tiếp bỏ qua outbox — tức thời về mặt UX nhưng vẫn nhất quán/audit được (FR-G-01/02, architecture.md §5.2); mỗi deployment gắn snapshot version audit được (FR-G-03). HTML gốc tại hostname set `Cache-Control` ngắn/`no-store` ở Cache API (không cache dài như asset tĩnh) để rollback có hiệu lực ngay không cần purge riêng."
- [x] "Implement wildcard subdomain (validate trùng + reserved words, FR-G-01) và SEO tự động: sitemap.xml + robots.txt per subdomain, canonical, OG image từ thumbnail, JSON-LD từ product/course gắn campaign (FR-G-05)."
- [x] "Setup Lighthouse CI trong `tooling/lighthouse-ci` chạy trên mỗi deployment mẫu — gate NFR-01 (Performance/SEO/BestPractices/A11y ≥95 mobile, LCP <1.8s 4G VN, JS runtime ≤10KB gzip)." **— checkbox này trước đó bị tick mà `tooling/` chỉ có 1 dòng README "chưa implement" (đã verify trực tiếp: `tooling/lighthouse-ci` không tồn tại) — cùng loại lỗi với #90/#92 đã nói ở trên. Đã implement thật lần này: `tooling/lighthouse-ci/run.ts` (workspace mới `@dv/lighthouse-ci`, thêm `tooling/*` vào `workspaces.packages` gốc, deps `lighthouse`/`chrome-launcher` lấy từ `catalog:server` sẵn có, không version rời) — launch Chrome headless qua `chrome-launcher`, chạy `lighthouse` mobile vào 1 URL landing mẫu, chấm 4 category + LCP, cộng thêm check dung lượng gzip `apps/landing-runtime/dist/index.iife.js` ≤10KB; fail (`process.exit(1)`) nếu bất kỳ ngưỡng nào không đạt. Đã test thật bằng `bun run tooling/lighthouse-ci/run.ts https://example.com` — chạy Chrome thật, ra điểm thật, gate fail đúng khi seo <95. `lint`/`typecheck`/`fmt` xanh. Đã thêm bước gọi script này vào `deploy-staging.yml` (sau bước deploy Dashboard). **Cố ý chưa làm**: chưa có script seed publish sẵn 1 "landing mẫu" cố định trên staging (repo chưa có seed script nào cho landing, đây là phần "seed" riêng nói ở `tooling/README.md`, không phải phạm vi Lighthouse CI) — nên bước gọi trong workflow được guard bằng `if: vars.SAMPLE_LANDING_URL != ''`, tức **skip, không fail**, cho tới khi ai đó publish 1 landing thật và set biến này (đã ghi hướng dẫn ở `docs/runbooks/ci-cd-setup.md` §7). "Chạy trên mỗi deployment mẫu" hiện đọc là "mỗi lần deploy staging chấm lại đúng 1 landing mẫu cố định", không phải chấm mọi deployment của mọi tenant — khớp cách đọc "gate chất lượng skill CWV" ở architecture.md §8, không phải NFR runtime per-tenant.**

**DoD Phase 3**: publish < 30s, Lighthouse ≥ 95 mobile trên landing mẫu, rollback tức thời.

---

## Phase 4 — Campaign/Product/Course + CRM

- [x] "CRUD Product (FR-C-01: tên, giá, mô tả, ảnh, `type` + JSONB attributes extensible) và Course (FR-C-02: product type=course + link nhóm Zalo, hướng dẫn kích hoạt, lịch khai giảng)."
- [x] "CRUD Campaign (FR-C-03/04): gắn n product, n landing, thời gian, mục tiêu, UTM mặc định, formConfig (fields bật, dropdown 'Bạn đang là ai?' tuỳ biến — screenshot #7), popupConfig, paymentConfig (VietQR account, template nội dung CK, bật/tắt SePay, link Zalo)."
- [x] "Implement `POST /public/leads` (FR-D-02/03): nhận field theo `campaign.formConfig` (họ tên, SĐT VN, email optional, persona 'Bạn đang là ai?', custom fields text/select/checkbox), validate SĐT VN, Turnstile invisible, rate limit IP + honeypot, dedupe theo SĐT trong org (FR-E-06). Bắt buộc checkbox consent thu thập dữ liệu cá nhân (mặc định không tick sẵn) ghi vào bảng `consents` (NFR-09, database-schema.md) — đây là yêu cầu tuân thủ Nghị định 13/2023/NĐ-CP, không phải optional."
- [x] "CRM: danh sách lead (filter campaign/product/trạng thái/UTM/ngày/người phụ trách, full-text search, phân trang server-side — FR-E-01), pipeline kanban kéo thả cấu hình theo org (FR-E-02)."
- [x] "Chi tiết lead: timeline activities, thông tin đơn + thanh toán, campaign nguồn (FR-E-03); assignment gán tay/round-robin theo campaign, sales chỉ thấy lead được gán nếu cấu hình vậy (FR-E-04); hành động nhanh `tel:`, copy SĐT, mở Zalo, xác nhận thanh toán, đánh dấu fulfilled (FR-E-05)."
- [x] "Analytics cơ bản campaign: views/submits/conversion rate theo ngày, đọc từ bảng `events` append-only (FR-C-05, architecture.md §8)."
- [x] "Digest email lead mới qua Resend (FR-I-03, P1): gộp lead mới trong khoảng cấu hình (mỗi giờ/cuối ngày) gửi người phụ trách/owner — tránh spam email từng lead, dùng domain/địa chỉ gửi đã setup ở Phase 0 prompt #7."
- [x] "Viết cross-tenant test cho toàn bộ endpoint campaign/product/lead vừa thêm (nối tiếp suite Phase 0, NFR-04)."

**DoD Phase 4**: flow landing → form → lead xuất hiện realtime trong CRM → sales thao tác đầy đủ.

---

## Phase 5 — Payment & fulfillment

> Tiền bạc — đường găng thứ 2 của dự án. Không rút gọn test cho idempotency/checksum-match/double-match.

- [x] "Implement UI 'Kết nối thanh toán' trong dashboard: org tự nhập thông tin tài khoản SePay của họ (bankBin, accountNumber, accountName, API key/webhook secret) → validate → mã hoá lưu `paymentConnections` (mô hình non-custodial, business-analysis.md §4.4) → trang hướng dẫn từng bước kèm ảnh (FR-D-15). **Đây là bước phải xong trước khi webhook ở prompt #3 có ý nghĩa** — không có kết nối thì không có secret để verify webhook."
- [x] "Implement tạo Order khi submit lead nếu campaign có sản phẩm trả phí: mã đơn `paymentConfig.transferPrefix` + 6 ký tự base32 (bỏ `0/O/1/I`) + 1 ký tự checksum (thuật toán ở database-schema.md ghi chú #3 — viết hàm tính/validate checksum riêng, có unit test), amount, nội dung CK = mã đơn đầy đủ, sinh VietQR hiển thị trong popup (FR-D-04)."
- [x] "Implement SePay webhook `/webhooks/sepay` (FR-D-05, architecture.md §7 threat model 'Webhook giả mạo SePay'): verify `Authorization: Apikey <secret>` tra theo `paymentConnections` của org, idempotent theo `providerTxId` (unique index). Trích mã đơn theo đúng 2 bước ở FR-D-05 — **không phải fuzzy match chịu lỗi chung chung**: bước 1 khớp tuyệt đối (mã + checksum hợp lệ, amount khớp CHÍNH XÁC, không có ngưỡng dung sai); bước 2 chỉ chạy khi bước 1 rỗng, sửa lỗi ký tự dễ nhầm rồi validate lại checksum, vẫn yêu cầu amount khớp chính xác + đúng 1 ứng viên. Không tìm được ứng viên duy nhất hoặc order đã `paid`/`refunded` từ trước (double-match, FR-D-14) → không auto-match, ghi `unmatchedTransactions` kèm `reason` — viết test riêng cho từng nhánh (`no_candidate`/`ambiguous`/`already_paid`), đây là nơi dễ sai nhất."
- [x] "Implement nút 'Tôi đã chuyển khoản' → order `awaiting_confirmation` + popup hướng dẫn + link Zalo; sales xác nhận trong CRM → `paid` (FR-D-06/08, có log + lý do khi đổi trạng thái). Email xác nhận `paid`/`fulfilled` cho lead qua Resend, tuỳ chọn bật/tắt theo campaign (FR-I-04, P1)."
- [x] "Implement `GET /public/orders/:code/status` poll (hoặc SSE) tối đa 10 phút, quá hạn hiện hướng dẫn manual (FR-D-07); rate limit theo IP + campaign (NFR-16, tránh 1 landing viral kéo sập backend polling); realtime dashboard qua Upstash pub/sub → SSE hub (architecture.md §5.3)."
- [x] "Màn hình đối soát: liệt kê `unmatchedTransactions` kèm lý do, ambiguous hiển thị order ứng viên xếp hạng theo độ khớp để sales chọn tay, ghi `matchType=manual` (FR-D-09, P1)."
- [x] "Implement flow hoàn tiền (FR-D-11..14): action 'Yêu cầu hoàn tiền' trên order detail → tạo `refundRequests` → checklist thủ công cho tenant tự chuyển khoản hoàn (nền tảng không giữ tiền, không tự động hoàn) → đánh dấu hoàn tất → order `refunded`. Double-match (giao dịch mới khớp vào order đã `paid`/`refunded`) tự động tạo `refundRequests` reason=`duplicate_payment` + badge cảnh báo CRM, không tự fulfillment lại — viết test riêng cho case double-match này."
- [x] "Hoàn thiện `landing-runtime` v2: render QR, poll status, chuỗi popup (đăng ký thành công → đang chờ/paid → hướng dẫn Zalo) — FR-D-01 đầy đủ."
- [x] "Viết test: webhook replay (gọi lại cùng `providerTxId`) không được tạo double-paid; chuyển khoản đúng mã đơn + checksum hợp lệ auto-match; amount lệch dù mã đúng **không** được match (không có ngưỡng dung sai — khác thiết kế cũ, xác nhận lại nếu Claude tự thêm tolerance)."

**DoD Phase 5**: chuyển khoản thật 10k → popup thành công tự bật ≤30s; webhook replay không double-paid; kịch bản manual đủ. _(Mốc dogfood — chạy khoá học thật của bạn trên nền tảng.)_

---

## Phase 6 — Import, Draw, hoàn thiện

- [x] "Implement import pipeline (FR-B-30): paste HTML / upload .html hoặc .zip / paste link artifact công khai → sanitize (strip script nguy hiểm, tracker lạ) → tách inline assets → generate srcmap → đặt tên layer tự động (heuristic + AI) → mở trong Studio. Chú ý threat model SSRF qua import URL (architecture.md §7): chỉ fetch qua proxy allowlist scheme/deny private IP. Ảnh trong HTML import trỏ URL ngoài không rõ nguồn: gắn cờ `pageAssets.unverifiedSource=true`, yêu cầu tenant tick xác nhận 'Tôi có quyền sử dụng ảnh này' trước khi publish landing chứa ảnh gắn cờ (FR-B-35)."
- [x] "Wizard 'chuẩn hoá phễu' sau import: AI đề xuất gắn form đăng ký chuẩn của nền tảng + meta SEO nếu thiếu (FR-B-31, P1)."
- [x] ~~"Implement Draw mode (FR-B-14, P1): vẽ annotation đè lên preview (mũi tên, khung, freehand) → chụp composite gửi AI kèm text."~~ **— XOÁ khỏi Phase 6, đã xong 100% ở Phase 1 (đã bị lặp — bản đầy đủ hơn nằm ở prompt Draw mode của Phase 1, dòng này là bản nháp cũ còn sót lại khi lên kế hoạch). Không có việc gì làm lại ở đây.**
- [x] "Custom domain tenant (FR-G-04, P1): CNAME + auto cert qua Cloudflare for SaaS, trạng thái verify hiển thị rõ."
- [x] "Audit log UI (FR-A-05, P1): mọi hành động ghi (publish, xoá lead, đổi trạng thái đơn...)."
- [x] "Export CSV theo filter lead (FR-E-07, P1); email notify lead mới (FR-E-08, P1)."
- [x] "Polish UX non-tech: mode đơn giản mặc định, onboarding checklist, empty state tiếng Việt tử tế — không cần agent, tự làm trực tiếp cho phần copy/UI này." **— checkbox này cũng từng bị tick mà chưa làm (đã verify: không có toggle/default nào cho "đơn giản", không có checklist, `platform-orgs-page.tsx` còn hardcode tiếng Anh) — cùng loại lỗi với #106/#149. Đã implement thật lần này**: (1) sidebar Layers/Inspect trong Studio mặc định **collapsed** (`studio-page.tsx` — trước đó comment ghi "collapsed by default" nhưng state khởi tạo thật là `false`/mở, một bug lệch ý định — đổi thành `true`; non-tech mặc định chỉ thấy Chat + Canvas, Layers/Inspect là công cụ nâng cao tự mở khi cần); (2) `OnboardingChecklist` mới (`features/studio/components/onboarding-checklist.tsx`) hiện ở trang `/landings` (cả empty-state lẫn gallery đã có landing): 4 bước "Tạo landing đầu tiên / Xuất bản / Kết nối AI / Kết nối thanh toán", tính trực tiếp từ query đã có sẵn (không thêm bảng/endpoint mới), ẩn khi xong hết hoặc dismiss (dismiss lưu theo org qua `usePersistentState`, tái dùng hook có sẵn thay vì viết mới); (3) rà lại toàn bộ empty state tiếng Việt hiện có — phần lớn đã tử tế từ trước (`products`/`campaigns`/`domains`/`leads`/`members`/`ai-connections`/`payment-connections`/`reconciliation`/Studio panels), không sửa gì thêm ở tenant-facing vì đã đạt; `platform-orgs-page.tsx` (English) cố ý **không** đổi vì đây là màn hình `/platform` chỉ dành cho chủ nền tảng (tech), không phải bề mặt non-tech persona P1 — khác phạm vi checkbox này. Phát hiện phụ: `apps/dashboard/src/paraglide/` (thư mục compiled, committed theo tech-stack.md) đã lệch hẳn so với `messages/{en,vi}.json` từ trước (nhiều key `studio*`/`onboarding*` đã dùng trong code nhưng thiếu trong bản compiled) — khiến `tsc -b` fail độc lập với thay đổi ở đây; đã chạy `paraglide-js compile` lại (đúng version pin trong catalog `2.24.1`, không phải bản `bunx` mặc định mới hơn — bản mới tạo diff runtime.js sai lệch, đã revert lần đầu) để đồng bộ lại. `lint`/`fmt`/`typecheck`/`build` xanh sau khi sửa."
- [x] "Implement quyền dữ liệu cá nhân cho lead (NFR-10, tuân thủ Nghị định 13/2023/NĐ-CP): quy trình yêu cầu xoá/xuất dữ liệu cá nhân (endpoint hoặc email hỗ trợ có SLA 72h) → anonymize `phone`/`email`/`customFields`, giữ `orders`/`payments` ẩn danh nếu đã có giao dịch. Job retention định kỳ (NFR-11): lead chưa từng `paid` không hoạt động sau 12 tháng → tự động anonymize, tenant tắt được qua `organizations.settings` — **khác** với job prune `pageVersions` ở backlog, đây là dữ liệu lead."
- [x] "Chạy pentest checklist OWASP top 10 áp threat model architecture.md §7 (review bảo mật đoạn xử lý payment/auth/upload — nói rõ từ 'bảo mật' để route đúng agent); load test webhook SePay + public endpoints, xác nhận API p95 < 300ms trừ AI streaming và webhook xử lý < 2s (NFR-03)."
- [x] "Xác nhận backup & uptime: Neon PITR bật, R2 versioning bật cho bucket deployments (NFR-06); đo lại NFR-02 (TTI dashboard, route transition) trên build production thật, không chỉ dev — sửa nếu lệch ngưỡng."

**DoD Phase 6**: học viên thật import HTML từ Claude.ai và publish không cần hỗ trợ; toàn bộ NFR-01..16 đã đo và đạt ngưỡng functional-requirements.md.

---

## Phase 7 (khi chạm ngưỡng infra-deployment-cost.md §2) — VPS & scale

- [ ] "Setup Dokploy stack trên VPS Việt Nam theo `docs/ops/infra-deployment-cost.md`; viết migration runbook CF→VPS cho api + DB."
- [ ] "Đổi driver `jobs` sang BullMQ, `storage` sang S3-compatible, `realtime` sang Redis pub/sub — chỉ đổi impl trong `packages/drivers`, business code không đổi (đúng nguyên tắc portable architecture.md §1 nguyên tắc #2)." **— `storage` đã có 1 driver đứng tạm cho runtime Bun từ Phase 0 (`local-fs.ts`, filesystem local, xem ghi chú ở prompt `packages/drivers` Phase 0) — driver đó chỉ để local dev chạy được, thay hẳn bằng driver S3 thật ở đây, không giữ song song hai driver cho cùng runtime."
- [ ] "Thumbnail/screenshot server-side qua Playwright chạy job VPS (thay browser rendering CF)."
- [ ] "Setup monitoring Grafana cho VPS (thay CF analytics)."

---

## Roadmap / backlog P2 (sau Phase 6, review theo implementation-plan.md — "scope creep → backlog P2")

Không chạy các prompt này trước khi hết Phase 6 (đúng đối sách rủi ro "scope creep" trong implementation-plan.md — Phase 7/VPS chỉ chạy khi chạm ngưỡng infra, không phải điều kiện để mở backlog P2) — nhưng viết sẵn ở đây để không phải phân tích lại docs khi tới lúc cần:

- [ ] "Implement 2FA TOTP cho owner/admin (FR-A-06)."
- [ ] "Implement voice input Web Speech API cho chat sidebar (FR-B-25)."
- [ ] "Implement marketplace skill — chia sẻ skill giữa tenant (FR-F-05)."
- [ ] "Thêm `product_type` mới (event, membership...) chỉ qua attribute schema, không đổi DB (FR-C-06) — dùng làm test cho tính extensible đã thiết kế ở Phase 4."
- [ ] "Thêm payment provider VNPAY/MoMo/Casso/PayOS qua interface `payments` đã có ở `packages/drivers` (FR-D-10) — chỉ thêm impl mới + trang hướng dẫn riêng (FR-D-15), không đổi interface. Ưu tiên theo nhu cầu tenant thật, không làm trước khi có ai cần."
- [ ] "Zalo OA/Telegram webhook cho thông báo lead mới (FR-E-08 P2 — phần email digest P1 đã làm ở Phase 4 prompt #6b)."
- [ ] "Tags, saved views, bulk actions cho CRM lead list (FR-E-09)."
- [ ] "Đăng ký + tích hợp chương trình chính thức 'Sign in with Claude'/'Sign in with ChatGPT' nếu được duyệt (FR-H-04) — đọc kỹ ràng buộc ToS ở ai-integration-byok.md trước khi code, đây là quyết định business trước, không phải việc kỹ thuật thuần."
- [ ] "Analytics nhẹ first-party không cookie bên thứ ba, đếm ở edge (FR-G-06, P1 — có thể làm sớm hơn nếu cần data cho sales pitch)."
- [ ] "Migrate video landing sang Bunny Stream cho landing vượt ~5GB egress-tương-đương/tháng (NFR-15) — thêm driver `video` mới trong `packages/drivers`, R2 raw file giữ nguyên làm mặc định cho landing nhẹ."
- [ ] "Job prune `pageVersions` trung gian (không phải deployment hiện tại, không có label) sau 90 ngày hoặc khi vượt 50 version/page (infra-deployment-cost.md §2, database-schema.md ghi chú #1) — set `prunedAt` + xoá object R2, giữ row Postgres."

## Việc không phải prompt code (nhắc lại từ implementation-plan.md, làm trước hoặc song song Phase 0)

- [x] Chốt tên miền nền tảng.
- [ ] Đăng ký developer interest "Sign in with ChatGPT" + liên hệ Anthropic về usage credits.
- [ ] Đăng ký SePay + mở tài khoản test webhook.
- [ ] Viết nháp nội dung 4 platform skill (dùng ở prompt Phase 2 #9) — đây là moat, không phải việc AI viết hộ.
- [ ] Soạn Chính sách bảo mật + điều khoản xử lý dữ liệu cá nhân (DPA) công bố nền tảng là Bên xử lý dữ liệu, tenant là Bên kiểm soát dữ liệu; disclose việc lưu trữ dữ liệu ở hạ tầng nước ngoài (Cloudflare/Neon) — NFR-12/13, functional-requirements.md. Việc pháp lý/văn bản, không phải prompt code.
