/** `campaignPaymentConfigSchema.transferPrefix` fallback — shared by order creation (`/public/leads`) and SePay webhook matching, which must agree on the same default. */
export const DEFAULT_TRANSFER_PREFIX = "DV";
